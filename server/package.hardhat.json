{
  "name": "game_blockchain_hardhat",
  "version": "1.0.0",
  "description": "Hardhat configuration for blockchain item trading",
  "scripts": {
    "compile": "hardhat compile",
    "node": "hardhat node",
    "deploy:localhost": "hardhat run scripts/deploy-contract.js --network localhost",
    "console": "hardhat console --network localhost"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0"
  }
}
