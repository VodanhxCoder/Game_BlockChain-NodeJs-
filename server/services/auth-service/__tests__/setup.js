/**
 * Jest Setup File
 * Configure Jest for running tests
 */

// Set NODE_ENV to test
process.env.NODE_ENV = 'test';

// Add any global test configuration here
global.testUtils = {
  // Helper functions for tests can go here if needed
};
